# ~/.aliases

# This is a helper for defining shortcuts and overriding default command arguments.
# It can be used in places like ~/.shrc to set up a friendly shell environment.

# Make "grep" output with colors by default.
alias grep='grep --color=auto';

# Make "ls" show hidden files.
alias ls='ls -A';
# Make "dir" show permissions and friendly file sizes.
alias dir='ls -lh';

# Helpers for fixing permissions issues.
alias own='sudo chown "$(whoami)"';
alias weown='sudo chgrp "$(id -ng)"';
alias we-own='weown';
